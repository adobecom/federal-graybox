federal/libs/global-navigation/dist/** linguist-generated=true

# Build artifacts are regenerated by the `rebuild-dist` workflow on every
# push to `main` and `stage`, so any textual conflict here is meaningless —
# the source of truth is the source tree, not the committed bundle.
#
#   merge=ours  → on conflict, keep HEAD's copy instead of failing the merge.
#                 The follow-up workflow then rebuilds and pushes the correct
#                 artifact, so "which side wins the merge" doesn't matter.
#   -diff       → don't render textual diffs for these files (they're huge
#                 minified single-line blobs that nobody can read anyway).
federal/libs/global-navigation/dist/main.js     merge=ours -diff
federal/libs/global-navigation/dist/main.js.map merge=ours -diff
