{
  "name": "figma-plugin-boilerplate",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/figma-plugin-boilerplate/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/figma-plugin-boilerplate"],
      "options": {
        "command": "node apps/figma-plugin-boilerplate/scripts/build.mjs",
        "cwd": "{workspaceRoot}"
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "cd dist/apps/figma-plugin-boilerplate && zip -r ../figma-plugin-boilerplate.zip .",
        "cwd": "{workspaceRoot}"
      }
    }
  },
  "tags": ["scope:figma", "type:app"]
}
