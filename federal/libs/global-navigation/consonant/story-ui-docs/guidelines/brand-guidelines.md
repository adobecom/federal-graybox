# Brand Guidelines

- Always use Adobe Clean (`hero background`, `lockup`, and `buttons`) via existing components.
- Use our saved hero background (`packages/components/src/hero-marquee/assets/hero-background.png`) unless explicitly overridden.
