# Responsive Design

- Hero/lockup components center content and wrap at 1200px container width.
- Use flexbox with `gap: var(--s2a-spacing-*)` tokens; never hard-coded px values.
- Spacer tokens: `--s2a-spacing-4xl` (desktop vertical), `--s2a-spacing-lg` (desktop CTA gaps).
